#include "headers.h"

/** 
 * Display Functions : contient toutes les fonctions d'affichage
*/

void dispMatrix(int nbL, int nbC, char **mat);
int deployMenu();
void dispFile(FILE *file);
void playGame_easyMode(int playerchoice);
void printSwitch(char c);